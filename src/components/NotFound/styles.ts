import { Styles } from "../../types";

export const styles: Styles = {
  link: {
    a: {
      color: (theme) => theme.palette.primary.main,
      textDecoration: "none",
      fontSize: "1rem",
    },
  },
};
